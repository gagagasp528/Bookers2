<h1>User info</h1>
<table class="table">
  <tbody>
    <tr><p><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></p>
      <th>name</th>
	  <th><%= user.name %></th>
	   <th>introduction</th>
	  <th><%= user.introduction %></th>
	  <th><%= link_to "edit"  %></th>
</tbody>
</table>

<h1>New book</h1>


<% if book.errors.any? %>
  <%= book.errors.count %> prohibited this book from being saved:
    <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
  <% end %>
<% end %>

<%= form_with model:book, local:true do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit"Create Book" %>
  <% end %>