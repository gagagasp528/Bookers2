  <div class="container">
   <div class="row">
       <div class="col-md-3">
         <%= render 'layouts/user',user: @user %>
         <%= render 'layouts/book',book: @book %>
       </div>
       <div class="col-md-8 offset-1">
           
  	<h2>Book detail</h2>
  	 <table class="table table-hover table-inverse">
  	 <thead>
    <tr>
       <th><%= attachment_image_tag @user, :profile_image, :fill, 20, 20, format: "jpeg", fallback: "no_image.jpg", size: "60x60" %></th>
       <th>t<%= @book.title %></th>
       <th><%= @book.body %></th>
       <% if @user.id == current_user.id %>
        <th><%= link_to "edit", edit_book_path, class:"btn btn-sm btn-success" %><th>
        <th><%= link_to "Destroy", book_path(@book), class:"btn btn-sm btn-danger", method: :delete, "data-confirm" => "Are you sure?" %></th>
       <% end %>    
     <tr>
        

     </thead>
     </table>
     </div>
       </div>
         </div>





