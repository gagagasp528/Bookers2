<h1>User info</h1>
<%= form_with model: @user, local:true do |f| %>
  <h4>Name</h4>
  <%= f.text_field : %>
  <h4>image</h4>
  <%= f.attachment_field :image %>
  <h4>introducation</h4>
  <%= f.text_area :caption %>
  <%= f.submit 'Update User' %>
<% end %>
