<%= render 'books/aaa',user: current_user, book: @book %>

  <div class="col-md-8 offset-md-1">
    <h2>Users</h2>

    <table class="table">
     <thead>
      <% @users.each do |user| %>
     <tr>
          <td><img class="attachment user profile_image fallback"><th><%= user.image %></th><td>
        <th><%= user.name %></th>
        <th colspan="3"></th>
        <td><a class="user_5328"></a><%= link_to "show", user_path(current_user) %></a>

        </tr>

      </thead>
    <tbody>
</div>
<% end %>










